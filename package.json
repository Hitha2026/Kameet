{
  "name": "pong-network-arena",
  "version": "2.1.0",
  "private": true,
  "description": "Arabic Pong + Breakout with mirrored brick formations, AI, private rooms, matchmaking and accounts.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.8.1"
  }
}
